// import { fromJS } from 'immutable';
// import { makeSelectClientsDomain } from '../selectors';

// const selector = makeSelectClientsDomain();

describe('makeSelectClientsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
