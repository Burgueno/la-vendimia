// import { fromJS } from 'immutable';
// import { makeSelectProductsDomain } from '../selectors';

// const selector = makeSelectProductsDomain();

describe('makeSelectProductsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
