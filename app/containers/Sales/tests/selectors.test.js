// import { fromJS } from 'immutable';
// import { makeSelectSalesDomain } from '../selectors';

// const selector = makeSelectSalesDomain();

describe('makeSelectSalesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
