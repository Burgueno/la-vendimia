// import { fromJS } from 'immutable';
// import { makeSelectNewSaleDomain } from '../selectors';

// const selector = makeSelectNewSaleDomain();

describe('makeSelectNewSaleDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
